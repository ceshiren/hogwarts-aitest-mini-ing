
server:
  port: 8091
  servlet:
    session:
      timeout: 30m #30分钟，测试的话时间不能太短，否则不准。m是分钟单位，s是秒的单位，h是小时的单位，不写单位的话默认是毫秒

spring:
  application:
    name: aitest
  datasource:
    url: jdbc:mysql://stuq.ceshiren.com:13306/aitest_mini6?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: hogwarts2020
    password: aitest@hogwarts2020
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.hogwartsmini.demo.entity
  configuration:
    mapUnderscoreToCamelCase: true

logging:
  level:
    com.hogwartsmini.demo.dao: debug #打印sql

hogwarts:
  key1: Hogwarts232

jenkins:
  url: http://stuq.ceshiren.com:8080/
  username: hogwarts
  password: hogwarts123
  #命令运行的测试用例类型  1 文本 2 文件
  casetype: 2
  casesuffix: yml
  testcommand: hrun
  callbackurl: http://localhost:8091/
